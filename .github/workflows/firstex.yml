name: My first workflow
on: push

jobs:
    CI_CD_Workflow:
        runs-on: ubuntu-latest
        steps:
        -   name: Checkout Action
            uses: actions/checkout@v4
        -   name: Hello
            run: echo "Hello"
        -   name: List files
            run: ls


